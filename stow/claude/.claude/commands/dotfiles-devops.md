---
description: "Consult the dotfiles DevOps specialist for stow/bootstrap/symlink work"
argument-hint: "<question or task>"
---

When invoked with `$ARGUMENTS`, spawn the `dotfiles-devops-agent` via the Agent tool with `run_in_background: false`, passing `$ARGUMENTS` verbatim as the task prompt. Relay its recommendations and any file/command output back to the user directly.
